name: Build, publish, deploy para MonsterASP.NET e release

# Só roda em push na main (deploy de producao) ou manualmente pelo Actions.
# Branches de feature/PR nao disparam deploy.
on:
  push:
    branches: [main]
  workflow_dispatch:

# Dois merges seguidos na main nao podem rodar em paralelo: o deploy por FTP
# sobrescreve /wwwroot inteiro (duas execucoes juntas deixariam o site num
# estado misturado) e o calculo da proxima versao le as tags existentes — se
# as duas lessem a mesma tag, tentariam criar a mesma versao e uma falharia.
# cancel-in-progress: false porque o deploy ja iniciado deve terminar, nao ser
# abortado no meio de um upload.
concurrency:
  group: deploy-producao
  cancel-in-progress: false

env:
  DOTNET_VERSION: "9.0.x"
  SOLUTION_PATH: ControleFamiliarAPI.sln
  PROJECT_PATH: ControleFamiliarAPI/ControleFamiliarAPI.csproj
  PUBLISH_OUTPUT: ./publish

jobs:
  build_and_deploy:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Setup .NET
        uses: actions/setup-dotnet@v4
        with:
          dotnet-version: ${{ env.DOTNET_VERSION }}

      - name: Restore
        run: dotnet restore ${{ env.SOLUTION_PATH }}

      - name: Build
        run: dotnet build ${{ env.SOLUTION_PATH }} --configuration Release --no-restore

      # Se algum teste falhar, o workflow para aqui — Publish/Deploy não
      # rodam. ControleFamiliarAPI.Tests cobre autenticação, isolamento por
      # família e as regras de negócio mais sensíveis.
      - name: Test
        run: dotnet test ${{ env.SOLUTION_PATH }} --configuration Release --no-build

      # Diferente do padrão usado no Representantes_API: lá as migrations
      # rodam aqui no CI porque o banco (Postgres) aceita conexão externa.
      # O MSSQL free do MonsterASP.NET só aceita "Local access" — conexão
      # de dentro do próprio datacenter deles — então um runner do GitHub
      # nunca conseguiria alcançar o banco. Por isso a aplicação aplica as
      # migrations sozinha ao subir (ver Program.cs), e esse passo não existe
      # aqui.

      - name: Publish
        run: dotnet publish ${{ env.PROJECT_PATH }} -c Release -o ${{ env.PUBLISH_OUTPUT }}

      # MonsterASP.NET é hospedagem compartilhada: o painel não dá acesso a
      # variáveis de ambiente do servidor/IIS diretamente. Sem
      # ASPNETCORE_ENVIRONMENT definido, o ASP.NET Core assume "Production"
      # por padrão e carrega appsettings.Production.json por cima do
      # appsettings.json. O appsettings.Production.json versionado no repo
      # só tem placeholders vazios; aqui reescrevemos esse arquivo já
      # publicado com os valores reais vindos dos GitHub Secrets — eles
      # nunca ficam no código-fonte.
      - name: Injetar segredos de produção no appsettings.Production.json
        run: |
          jq --arg cs "${{ secrets.DB_CONNECTION_STRING }}" \
             --arg jwt "${{ secrets.JWT_KEY }}" \
             --arg origin "${{ secrets.WEB_ORIGIN }}" \
             --arg scalarUser "${{ secrets.SCALAR_USERNAME }}" \
             --arg scalarPassword "${{ secrets.SCALAR_PASSWORD }}" \
             --arg smtpHost "${{ secrets.SMTP_HOST }}" \
             --arg smtpPort "${{ secrets.SMTP_PORT }}" \
             --arg smtpUsername "${{ secrets.SMTP_USERNAME }}" \
             --arg smtpPassword "${{ secrets.SMTP_PASSWORD }}" \
             --arg smtpFrom "${{ secrets.SMTP_FROM }}" \
             '.ConnectionStrings.DefaultConnection = $cs
              | .Jwt.Key = $jwt
              | .Cors.AllowedOrigins = [$origin]
              | .Scalar.Username = $scalarUser
              | .Scalar.Password = $scalarPassword
              | .Frontend.BaseUrl = $origin
              | .Smtp.Host = $smtpHost
              | .Smtp.Port = (if $smtpPort == "" then 587 else ($smtpPort | tonumber) end)
              | .Smtp.Username = $smtpUsername
              | .Smtp.Password = $smtpPassword
              | .Smtp.From = $smtpFrom' \
             ${{ env.PUBLISH_OUTPUT }}/appsettings.Production.json > ${{ env.PUBLISH_OUTPUT }}/appsettings.Production.json.tmp
          mv ${{ env.PUBLISH_OUTPUT }}/appsettings.Production.json.tmp ${{ env.PUBLISH_OUTPUT }}/appsettings.Production.json

      # Libera os locks de arquivo do IIS antes de sobrescrever o app.
      - name: Take app offline (release IIS file locks)
        run: |
          echo "App temporarily offline for deployment." > app_offline.htm
          curl --ftp-pasv -T app_offline.htm --user "${{ secrets.FTP_USERNAME }}:${{ secrets.FTP_PASSWORD }}" "ftp://${{ secrets.FTP_SERVER }}/wwwroot/app_offline.htm"

          # O IIS nao solta os handles no instante em que app_offline.htm
          # aparece: ele ainda precisa drenar as requisicoes em andamento e
          # descarregar o processo pra liberar ControleFamiliarAPI.dll.
          #
          # Sem esta espera o passo seguinte tentava substituir a DLL um
          # segundo depois do upload e tomava "550 Could not access file:
          # driver error: calling GetHandle: failure" logo no primeiro
          # arquivo. Foi o que derrubou a producao em 13/08/2026: dois merges
          # na main com um minuto de diferenca pegaram o app recem-reiniciado
          # pelo deploy anterior, com o assembly ainda carregado.
          sleep 20

      - name: FTP Deploy
        uses: SamKirkland/FTP-Deploy-Action@v4.3.5
        with:
          server: ${{ secrets.FTP_SERVER }}
          username: ${{ secrets.FTP_USERNAME }}
          password: ${{ secrets.FTP_PASSWORD }}
          local-dir: ./publish/
          server-dir: /wwwroot/

      # if: always() e o ponto deste passo. Sem ele, qualquer falha no FTP
      # Deploy deixa o app_offline.htm no servidor e o site responde 503 pra
      # todo mundo ATE alguem perceber e intervir — foi exatamente assim que
      # uma falha de 3 segundos no upload virou uma producao fora do ar.
      #
      # Um deploy que falha deve deixar no ar o que ja estava la, nao uma
      # pagina de manutencao permanente. O caso comum de falha e o sync
      # morrer no primeiro arquivo (nada foi trocado ainda), e ai voltar
      # online restaura exatamente a versao anterior.
      #
      # O risco assumido e o oposto: se o sync morrer no meio, o app volta
      # com arquivos de duas versoes misturados e pode nao subir. Fica
      # visivel do mesmo jeito (o job aparece vermelho e o job release nao
      # roda, entao nenhuma tag e criada pra essa versao), e o conserto e
      # reexecutar o deploy — enquanto o 503 silencioso nao tem sintoma
      # nenhum alem do site fora.
      - name: Bring app back online
        if: always()
        run: |
          curl --ftp-pasv --user "${{ secrets.FTP_USERNAME }}:${{ secrets.FTP_PASSWORD }}" -Q "DELE /wwwroot/app_offline.htm" "ftp://${{ secrets.FTP_SERVER }}/"

  # A Release e criada num job separado que depende do deploy: se o build, os
  # testes ou o upload por FTP falharem, nenhuma versao e publicada. Assim toda
  # tag no GitHub corresponde exatamente ao que esta no ar em fiscalhub.runasp.net,
  # e nao apenas a um commit que entrou na main.
  release:
    needs: build_and_deploy
    runs-on: ubuntu-latest

    # Criar tag e Release exige escrita no repositorio. O job de deploy continua
    # com a permissao padrao (somente leitura) — so este precisa de contents: write.
    permissions:
      contents: write

    # Somente push na main gera versao nova. Um workflow_dispatch apontado para
    # uma tag antiga (o procedimento de rollback documentado no README) reimplanta
    # aquela versao sem inventar um numero novo em cima dela.
    if: github.event_name == 'push'

    steps:
      # fetch-depth: 0 traz o historico completo e as tags — sem isso o passo
      # seguinte nao enxerga a ultima versao publicada e recomecaria do v1.0.0.
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Calcular a proxima versao
        id: versao
        run: |
          # --sort=-v:refname ordena por versao (nao alfabeticamente), entao a
          # primeira linha e a maior tag. O filtro exige o formato vN.N.N para
          # ignorar qualquer tag manual fora do padrao.
          ultima=$(git tag --list 'v[0-9]*.[0-9]*.[0-9]*' --sort=-v:refname | head -n 1 || true)

          if [ -z "$ultima" ]; then
            proxima="v1.0.0"
          else
            numeros="${ultima#v}"
            maior="${numeros%%.*}"
            resto="${numeros#*.}"
            menor="${resto%%.*}"
            patch="${resto##*.}"
            proxima="v${maior}.${menor}.$((patch + 1))"
          fi

          echo "Ultima versao: ${ultima:-nenhuma} -> nova: $proxima"
          echo "tag=$proxima" >> "$GITHUB_OUTPUT"

      # --generate-notes monta o corpo da release a partir dos PRs mergeados
      # desde a release anterior. --target fixa a tag no commit que acabou de ser
      # implantado, e nao no que estiver na ponta da main quando isso rodar.
      - name: Criar a Release
        env:
          GH_TOKEN: ${{ github.token }}
        run: |
          gh release create "${{ steps.versao.outputs.tag }}" \
            --target "$GITHUB_SHA" \
            --title "${{ steps.versao.outputs.tag }}" \
            --generate-notes
